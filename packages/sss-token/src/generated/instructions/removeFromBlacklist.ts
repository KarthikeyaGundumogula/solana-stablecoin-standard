/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { STC_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const REMOVE_FROM_BLACKLIST_DISCRIMINATOR = new Uint8Array([
  47, 105, 20, 10, 165, 168, 203, 219,
]);

export function getRemoveFromBlacklistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_FROM_BLACKLIST_DISCRIMINATOR,
  );
}

export type RemoveFromBlacklistInstruction<
  TProgram extends string = typeof STC_PROGRAM_PROGRAM_ADDRESS,
  TAccountBlacklister extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoleAccount extends string | AccountMeta<string> = string,
  TAccountAddressToRemove extends string | AccountMeta<string> = string,
  TAccountBlacklistEntry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBlacklister extends string
        ? WritableSignerAccount<TAccountBlacklister> &
            AccountSignerMeta<TAccountBlacklister>
        : TAccountBlacklister,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoleAccount extends string
        ? ReadonlyAccount<TAccountRoleAccount>
        : TAccountRoleAccount,
      TAccountAddressToRemove extends string
        ? ReadonlyAccount<TAccountAddressToRemove>
        : TAccountAddressToRemove,
      TAccountBlacklistEntry extends string
        ? WritableAccount<TAccountBlacklistEntry>
        : TAccountBlacklistEntry,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveFromBlacklistInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RemoveFromBlacklistInstructionDataArgs = {};

export function getRemoveFromBlacklistInstructionDataEncoder(): FixedSizeEncoder<RemoveFromBlacklistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REMOVE_FROM_BLACKLIST_DISCRIMINATOR,
    }),
  );
}

export function getRemoveFromBlacklistInstructionDataDecoder(): FixedSizeDecoder<RemoveFromBlacklistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRemoveFromBlacklistInstructionDataCodec(): FixedSizeCodec<
  RemoveFromBlacklistInstructionDataArgs,
  RemoveFromBlacklistInstructionData
> {
  return combineCodec(
    getRemoveFromBlacklistInstructionDataEncoder(),
    getRemoveFromBlacklistInstructionDataDecoder(),
  );
}

export type RemoveFromBlacklistInput<
  TAccountBlacklister extends string = string,
  TAccountConfig extends string = string,
  TAccountRoleAccount extends string = string,
  TAccountAddressToRemove extends string = string,
  TAccountBlacklistEntry extends string = string,
> = {
  /** The blacklister (must hold Blacklister role) */
  blacklister: TransactionSigner<TAccountBlacklister>;
  /** The stablecoin config PDA */
  config: Address<TAccountConfig>;
  /** The blacklister's role PDA */
  roleAccount: Address<TAccountRoleAccount>;
  /** The address to remove from blacklist */
  addressToRemove: Address<TAccountAddressToRemove>;
  /** The blacklist entry PDA (will be closed / reclaimed) */
  blacklistEntry: Address<TAccountBlacklistEntry>;
};

export function getRemoveFromBlacklistInstruction<
  TAccountBlacklister extends string,
  TAccountConfig extends string,
  TAccountRoleAccount extends string,
  TAccountAddressToRemove extends string,
  TAccountBlacklistEntry extends string,
  TProgramAddress extends Address = typeof STC_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RemoveFromBlacklistInput<
    TAccountBlacklister,
    TAccountConfig,
    TAccountRoleAccount,
    TAccountAddressToRemove,
    TAccountBlacklistEntry
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveFromBlacklistInstruction<
  TProgramAddress,
  TAccountBlacklister,
  TAccountConfig,
  TAccountRoleAccount,
  TAccountAddressToRemove,
  TAccountBlacklistEntry
> {
  // Program address.
  const programAddress = config?.programAddress ?? STC_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    blacklister: { value: input.blacklister ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    roleAccount: { value: input.roleAccount ?? null, isWritable: false },
    addressToRemove: {
      value: input.addressToRemove ?? null,
      isWritable: false,
    },
    blacklistEntry: { value: input.blacklistEntry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("blacklister", accounts.blacklister),
      getAccountMeta("config", accounts.config),
      getAccountMeta("roleAccount", accounts.roleAccount),
      getAccountMeta("addressToRemove", accounts.addressToRemove),
      getAccountMeta("blacklistEntry", accounts.blacklistEntry),
    ],
    data: getRemoveFromBlacklistInstructionDataEncoder().encode({}),
    programAddress,
  } as RemoveFromBlacklistInstruction<
    TProgramAddress,
    TAccountBlacklister,
    TAccountConfig,
    TAccountRoleAccount,
    TAccountAddressToRemove,
    TAccountBlacklistEntry
  >);
}

export type ParsedRemoveFromBlacklistInstruction<
  TProgram extends string = typeof STC_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The blacklister (must hold Blacklister role) */
    blacklister: TAccountMetas[0];
    /** The stablecoin config PDA */
    config: TAccountMetas[1];
    /** The blacklister's role PDA */
    roleAccount: TAccountMetas[2];
    /** The address to remove from blacklist */
    addressToRemove: TAccountMetas[3];
    /** The blacklist entry PDA (will be closed / reclaimed) */
    blacklistEntry: TAccountMetas[4];
  };
  data: RemoveFromBlacklistInstructionData;
};

export function parseRemoveFromBlacklistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveFromBlacklistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      blacklister: getNextAccount(),
      config: getNextAccount(),
      roleAccount: getNextAccount(),
      addressToRemove: getNextAccount(),
      blacklistEntry: getNextAccount(),
    },
    data: getRemoveFromBlacklistInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
