/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { STC_PROGRAM_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const ADD_TO_BLACKLIST_DISCRIMINATOR = new Uint8Array([
  90, 115, 98, 231, 173, 119, 117, 176,
]);

export function getAddToBlacklistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADD_TO_BLACKLIST_DISCRIMINATOR,
  );
}

export type AddToBlacklistInstruction<
  TProgram extends string = typeof STC_PROGRAM_PROGRAM_ADDRESS,
  TAccountBlacklister extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoleAccount extends string | AccountMeta<string> = string,
  TAccountAddressToBlacklist extends string | AccountMeta<string> = string,
  TAccountBlacklistEntry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBlacklister extends string
        ? WritableSignerAccount<TAccountBlacklister> &
            AccountSignerMeta<TAccountBlacklister>
        : TAccountBlacklister,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoleAccount extends string
        ? ReadonlyAccount<TAccountRoleAccount>
        : TAccountRoleAccount,
      TAccountAddressToBlacklist extends string
        ? ReadonlyAccount<TAccountAddressToBlacklist>
        : TAccountAddressToBlacklist,
      TAccountBlacklistEntry extends string
        ? WritableAccount<TAccountBlacklistEntry>
        : TAccountBlacklistEntry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddToBlacklistInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AddToBlacklistInstructionDataArgs = {};

export function getAddToBlacklistInstructionDataEncoder(): FixedSizeEncoder<AddToBlacklistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADD_TO_BLACKLIST_DISCRIMINATOR }),
  );
}

export function getAddToBlacklistInstructionDataDecoder(): FixedSizeDecoder<AddToBlacklistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAddToBlacklistInstructionDataCodec(): FixedSizeCodec<
  AddToBlacklistInstructionDataArgs,
  AddToBlacklistInstructionData
> {
  return combineCodec(
    getAddToBlacklistInstructionDataEncoder(),
    getAddToBlacklistInstructionDataDecoder(),
  );
}

export type AddToBlacklistInput<
  TAccountBlacklister extends string = string,
  TAccountConfig extends string = string,
  TAccountRoleAccount extends string = string,
  TAccountAddressToBlacklist extends string = string,
  TAccountBlacklistEntry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The blacklister (must hold Blacklister role) */
  blacklister: TransactionSigner<TAccountBlacklister>;
  /** The stablecoin config PDA */
  config: Address<TAccountConfig>;
  /** The blacklister's role PDA */
  roleAccount: Address<TAccountRoleAccount>;
  /** The address to blacklist */
  addressToBlacklist: Address<TAccountAddressToBlacklist>;
  /** The blacklist entry PDA (created if it doesn't exist) */
  blacklistEntry: Address<TAccountBlacklistEntry>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAddToBlacklistInstruction<
  TAccountBlacklister extends string,
  TAccountConfig extends string,
  TAccountRoleAccount extends string,
  TAccountAddressToBlacklist extends string,
  TAccountBlacklistEntry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof STC_PROGRAM_PROGRAM_ADDRESS,
>(
  input: AddToBlacklistInput<
    TAccountBlacklister,
    TAccountConfig,
    TAccountRoleAccount,
    TAccountAddressToBlacklist,
    TAccountBlacklistEntry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddToBlacklistInstruction<
  TProgramAddress,
  TAccountBlacklister,
  TAccountConfig,
  TAccountRoleAccount,
  TAccountAddressToBlacklist,
  TAccountBlacklistEntry,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? STC_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    blacklister: { value: input.blacklister ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    roleAccount: { value: input.roleAccount ?? null, isWritable: false },
    addressToBlacklist: {
      value: input.addressToBlacklist ?? null,
      isWritable: false,
    },
    blacklistEntry: { value: input.blacklistEntry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.blacklister),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roleAccount),
      getAccountMeta(accounts.addressToBlacklist),
      getAccountMeta(accounts.blacklistEntry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddToBlacklistInstructionDataEncoder().encode({}),
    programAddress,
  } as AddToBlacklistInstruction<
    TProgramAddress,
    TAccountBlacklister,
    TAccountConfig,
    TAccountRoleAccount,
    TAccountAddressToBlacklist,
    TAccountBlacklistEntry,
    TAccountSystemProgram
  >);
}

export type ParsedAddToBlacklistInstruction<
  TProgram extends string = typeof STC_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The blacklister (must hold Blacklister role) */
    blacklister: TAccountMetas[0];
    /** The stablecoin config PDA */
    config: TAccountMetas[1];
    /** The blacklister's role PDA */
    roleAccount: TAccountMetas[2];
    /** The address to blacklist */
    addressToBlacklist: TAccountMetas[3];
    /** The blacklist entry PDA (created if it doesn't exist) */
    blacklistEntry: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: AddToBlacklistInstructionData;
};

export function parseAddToBlacklistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddToBlacklistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      blacklister: getNextAccount(),
      config: getNextAccount(),
      roleAccount: getNextAccount(),
      addressToBlacklist: getNextAccount(),
      blacklistEntry: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddToBlacklistInstructionDataDecoder().decode(instruction.data),
  };
}
