/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { STC_PROGRAM_PROGRAM_ADDRESS } from "../programs";
import {
  getRoleTypeDecoder,
  getRoleTypeEncoder,
  type RoleType,
  type RoleTypeArgs,
} from "../types";

export const UPDATE_ROLES_DISCRIMINATOR = new Uint8Array([
  220, 152, 205, 233, 177, 123, 219, 125,
]);

export function getUpdateRolesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ROLES_DISCRIMINATOR,
  );
}

export type UpdateRolesInstruction<
  TProgram extends string = typeof STC_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAssignee extends string | AccountMeta<string> = string,
  TAccountRoleAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAssignee extends string
        ? ReadonlyAccount<TAccountAssignee>
        : TAccountAssignee,
      TAccountRoleAccount extends string
        ? WritableAccount<TAccountRoleAccount>
        : TAccountRoleAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateRolesInstructionData = {
  discriminator: ReadonlyUint8Array;
  roleType: RoleType;
  isActive: boolean;
};

export type UpdateRolesInstructionDataArgs = {
  roleType: RoleTypeArgs;
  isActive: boolean;
};

export function getUpdateRolesInstructionDataEncoder(): FixedSizeEncoder<UpdateRolesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roleType", getRoleTypeEncoder()],
      ["isActive", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ROLES_DISCRIMINATOR }),
  );
}

export function getUpdateRolesInstructionDataDecoder(): FixedSizeDecoder<UpdateRolesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roleType", getRoleTypeDecoder()],
    ["isActive", getBooleanDecoder()],
  ]);
}

export function getUpdateRolesInstructionDataCodec(): FixedSizeCodec<
  UpdateRolesInstructionDataArgs,
  UpdateRolesInstructionData
> {
  return combineCodec(
    getUpdateRolesInstructionDataEncoder(),
    getUpdateRolesInstructionDataDecoder(),
  );
}

export type UpdateRolesInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountAssignee extends string = string,
  TAccountRoleAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Must be the master authority */
  authority: TransactionSigner<TAccountAuthority>;
  /** The stablecoin config PDA */
  config: Address<TAccountConfig>;
  /** The wallet being assigned or revoked a role */
  assignee: Address<TAccountAssignee>;
  /** The role PDA to create or update */
  roleAccount: Address<TAccountRoleAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  roleType: UpdateRolesInstructionDataArgs["roleType"];
  isActive: UpdateRolesInstructionDataArgs["isActive"];
};

export function getUpdateRolesInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountAssignee extends string,
  TAccountRoleAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof STC_PROGRAM_PROGRAM_ADDRESS,
>(
  input: UpdateRolesInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountAssignee,
    TAccountRoleAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateRolesInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountAssignee,
  TAccountRoleAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? STC_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    assignee: { value: input.assignee ?? null, isWritable: false },
    roleAccount: { value: input.roleAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("assignee", accounts.assignee),
      getAccountMeta("roleAccount", accounts.roleAccount),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUpdateRolesInstructionDataEncoder().encode(
      args as UpdateRolesInstructionDataArgs,
    ),
    programAddress,
  } as UpdateRolesInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountAssignee,
    TAccountRoleAccount,
    TAccountSystemProgram
  >);
}

export type ParsedUpdateRolesInstruction<
  TProgram extends string = typeof STC_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Must be the master authority */
    authority: TAccountMetas[0];
    /** The stablecoin config PDA */
    config: TAccountMetas[1];
    /** The wallet being assigned or revoked a role */
    assignee: TAccountMetas[2];
    /** The role PDA to create or update */
    roleAccount: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: UpdateRolesInstructionData;
};

export function parseUpdateRolesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateRolesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      assignee: getNextAccount(),
      roleAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdateRolesInstructionDataDecoder().decode(instruction.data),
  };
}
