/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { STC_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_SSS2_DISCRIMINATOR = new Uint8Array([
  55, 253, 209, 103, 146, 222, 48, 2,
]);

export function getInitializeSss2DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_SSS2_DISCRIMINATOR,
  );
}

export type InitializeSss2Instruction<
  TProgram extends string = typeof STC_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountTransferHookProgram extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMint extends string
        ? WritableSignerAccount<TAccountMint> & AccountSignerMeta<TAccountMint>
        : TAccountMint,
      TAccountTransferHookProgram extends string
        ? ReadonlyAccount<TAccountTransferHookProgram>
        : TAccountTransferHookProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeSss2InstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  symbol: string;
  uri: string;
  decimals: number;
};

export type InitializeSss2InstructionDataArgs = {
  name: string;
  symbol: string;
  uri: string;
  decimals: number;
};

export function getInitializeSss2InstructionDataEncoder(): Encoder<InitializeSss2InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["symbol", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["uri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["decimals", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_SSS2_DISCRIMINATOR }),
  );
}

export function getInitializeSss2InstructionDataDecoder(): Decoder<InitializeSss2InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["symbol", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["uri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["decimals", getU8Decoder()],
  ]);
}

export function getInitializeSss2InstructionDataCodec(): Codec<
  InitializeSss2InstructionDataArgs,
  InitializeSss2InstructionData
> {
  return combineCodec(
    getInitializeSss2InstructionDataEncoder(),
    getInitializeSss2InstructionDataDecoder(),
  );
}

export type InitializeSss2AsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountMint extends string = string,
  TAccountTransferHookProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  /**
   * Token-2022 mint with all SSS-2 extensions.
   * - MetadataPointer: on-chain metadata stored in the mint
   * - TransferHook: calls transfer_hook_program on every transfer
   * - PermanentDelegate: config PDA can seize tokens from any account
   * - DefaultAccountState: new token accounts start Frozen
   */
  mint: TransactionSigner<TAccountMint>;
  /** The transfer hook program that enforces blacklist checks */
  transferHookProgram: Address<TAccountTransferHookProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: InitializeSss2InstructionDataArgs["name"];
  symbol: InitializeSss2InstructionDataArgs["symbol"];
  uri: InitializeSss2InstructionDataArgs["uri"];
  decimals: InitializeSss2InstructionDataArgs["decimals"];
};

export async function getInitializeSss2InstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountMint extends string,
  TAccountTransferHookProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof STC_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitializeSss2AsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountMint,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeSss2Instruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountMint,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? STC_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    transferHookProgram: {
      value: input.transferHookProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            115, 116, 97, 98, 108, 101, 99, 111, 105, 110, 95, 99, 111, 110,
            102, 105, 103,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("mint", accounts.mint.value),
        ),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("transferHookProgram", accounts.transferHookProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeSss2InstructionDataEncoder().encode(
      args as InitializeSss2InstructionDataArgs,
    ),
    programAddress,
  } as InitializeSss2Instruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountMint,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type InitializeSss2Input<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountMint extends string = string,
  TAccountTransferHookProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  /**
   * Token-2022 mint with all SSS-2 extensions.
   * - MetadataPointer: on-chain metadata stored in the mint
   * - TransferHook: calls transfer_hook_program on every transfer
   * - PermanentDelegate: config PDA can seize tokens from any account
   * - DefaultAccountState: new token accounts start Frozen
   */
  mint: TransactionSigner<TAccountMint>;
  /** The transfer hook program that enforces blacklist checks */
  transferHookProgram: Address<TAccountTransferHookProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: InitializeSss2InstructionDataArgs["name"];
  symbol: InitializeSss2InstructionDataArgs["symbol"];
  uri: InitializeSss2InstructionDataArgs["uri"];
  decimals: InitializeSss2InstructionDataArgs["decimals"];
};

export function getInitializeSss2Instruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountMint extends string,
  TAccountTransferHookProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof STC_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitializeSss2Input<
    TAccountAuthority,
    TAccountConfig,
    TAccountMint,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeSss2Instruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountMint,
  TAccountTransferHookProgram,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? STC_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    transferHookProgram: {
      value: input.transferHookProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("transferHookProgram", accounts.transferHookProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeSss2InstructionDataEncoder().encode(
      args as InitializeSss2InstructionDataArgs,
    ),
    programAddress,
  } as InitializeSss2Instruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountMint,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeSss2Instruction<
  TProgram extends string = typeof STC_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    /**
     * Token-2022 mint with all SSS-2 extensions.
     * - MetadataPointer: on-chain metadata stored in the mint
     * - TransferHook: calls transfer_hook_program on every transfer
     * - PermanentDelegate: config PDA can seize tokens from any account
     * - DefaultAccountState: new token accounts start Frozen
     */
    mint: TAccountMetas[2];
    /** The transfer hook program that enforces blacklist checks */
    transferHookProgram: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: InitializeSss2InstructionData;
};

export function parseInitializeSss2Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeSss2Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      mint: getNextAccount(),
      transferHookProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeSss2InstructionDataDecoder().decode(instruction.data),
  };
}
