/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "gill";

export const BLACKLIST_ENTRY_DISCRIMINATOR = new Uint8Array([
  218, 179, 231, 40, 141, 25, 168, 189,
]);

export function getBlacklistEntryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BLACKLIST_ENTRY_DISCRIMINATOR,
  );
}

export type BlacklistEntry = {
  discriminator: ReadonlyUint8Array;
  /** The mint this blacklist entry is associated with */
  mint: Address;
  /** The blacklisted wallet address */
  blacklistedAddress: Address;
  /** Bump seed for this PDA */
  bump: number;
};

export type BlacklistEntryArgs = {
  /** The mint this blacklist entry is associated with */
  mint: Address;
  /** The blacklisted wallet address */
  blacklistedAddress: Address;
  /** Bump seed for this PDA */
  bump: number;
};

/** Gets the encoder for {@link BlacklistEntryArgs} account data. */
export function getBlacklistEntryEncoder(): FixedSizeEncoder<BlacklistEntryArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mint", getAddressEncoder()],
      ["blacklistedAddress", getAddressEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BLACKLIST_ENTRY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link BlacklistEntry} account data. */
export function getBlacklistEntryDecoder(): FixedSizeDecoder<BlacklistEntry> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mint", getAddressDecoder()],
    ["blacklistedAddress", getAddressDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link BlacklistEntry} account data. */
export function getBlacklistEntryCodec(): FixedSizeCodec<
  BlacklistEntryArgs,
  BlacklistEntry
> {
  return combineCodec(getBlacklistEntryEncoder(), getBlacklistEntryDecoder());
}

export function decodeBlacklistEntry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<BlacklistEntry, TAddress>;
export function decodeBlacklistEntry<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<BlacklistEntry, TAddress>;
export function decodeBlacklistEntry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<BlacklistEntry, TAddress> | MaybeAccount<BlacklistEntry, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBlacklistEntryDecoder(),
  );
}

export async function fetchBlacklistEntry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<BlacklistEntry, TAddress>> {
  const maybeAccount = await fetchMaybeBlacklistEntry(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBlacklistEntry<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<BlacklistEntry, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBlacklistEntry(maybeAccount);
}

export async function fetchAllBlacklistEntry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<BlacklistEntry>[]> {
  const maybeAccounts = await fetchAllMaybeBlacklistEntry(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBlacklistEntry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<BlacklistEntry>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeBlacklistEntry(maybeAccount),
  );
}

export function getBlacklistEntrySize(): number {
  return 73;
}
